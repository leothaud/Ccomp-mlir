int variable = 0;

float floatVar = 3.14;

union myUnion {
    int fieldA = 1, fieldB;
    char fieldC = variable;
};

enum myEnum {
    A, B, C
};

typedef union myUnion myUnion_t;

int main(int argc, char** argv) {
    int x = 17;
    int y;
    for (int i = 3; i < 6; ++i)
        printf(i);
//    printf(i);
    return y;
}