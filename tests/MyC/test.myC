int variable = 0;

float floatVar = 3.14;

union myUnion {
    int fieldA = 1, fieldB;
    char fieldC = variable;
};

enum myEnum {
    A, B, C
};

void printf(int i);

typedef char mytype1;
typedef mytype1 mytype2;
typedef float mytype3;

typedef union myUnion myUnion_t;

int main(int argc, char** argv) {
    mytype1 x = 17;
    int y = 3 * x + 1;
    char *s = "myString";
    for (int i = 3; i < 6; ++i)
        printf(i);
    do {
        ++y;
    } while( y < 16);
    return _Generic(x, char: 3, int:y, default:4);
}